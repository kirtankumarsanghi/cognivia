{
  "buildCommand": "cd frontend && npm install @rollup/rollup-linux-x64-gnu --save-optional && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install",
  "framework": "vite",
  "regions": ["iad1"]
}
